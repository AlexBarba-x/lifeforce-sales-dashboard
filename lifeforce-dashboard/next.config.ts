import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the Next.js dev-mode floating indicator (the "N" badge bottom-left).
  devIndicators: false,
};

export default nextConfig;
